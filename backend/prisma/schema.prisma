generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Řádek "url = ..." je zde od verze 7 ZAKÁZÁN
}

model Message {
  id        Int      @id @default(autoincrement())
  text      String
  user      String
  added     DateTime @default(now())
}